<?php
	function addNumbers(int $a, int $b) {
		return $a + $b;
	}
	
	echo addNumbers(5, "5 days"); // work on PHP < 7
	// since strict is NOT enabled "5 days" is changed to int(5), and it will return 10
?>